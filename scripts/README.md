# Repository scripts

Root `package.json` commands are the supported task interface for Sprint 1. Add a script here only when a cross-platform command cannot be expressed clearly through pnpm or uv; keep scripts small, tested and free of secrets.
