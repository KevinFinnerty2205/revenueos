import { PlaceholderPage } from "@/components/placeholder-page";

export default function TasksPage() {
  return (
    <PlaceholderPage
      eyebrow="Follow-through"
      title="Tasks"
      description="Tasks will bring approved next actions into one focused view."
      emptyTitle="Task workflows are not available yet"
      emptyDescription="Sprint 1 provides this navigation destination only. It does not store or generate tasks."
    />
  );
}
